export interface ComponentItem {
    id: string
    componentName: string
    componentCode: string
    componentType: string
    description?: string
    status: number
    sortNo: number
    version: number
    updatedAt?: string
}
